{% extends 'base.html.twig' %}

{% block body %}
    <h1>Developers list</h1>

    <table class="table table-striped">
        <thead>
            <tr>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for developer in developers %}
            <tr>
                <td>{{ developer.name }}</td>
                <td>
                    <a href="{{ path('developer_show', { 'id': developer.id }) }}" class="btn btn-info" role="button">show</a>
                    <a href="{{ path('developer_edit', { 'id': developer.id }) }}" class="btn btn-warning" role="button">edit</a>
                    <a href="{{ path('developer_delete', { 'id': developer.id }) }}" class="btn btn-error" role="button">delete</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('developer_new') }}" class="btn btn-success" role="button">Create a new developer</a>
{% endblock %}
