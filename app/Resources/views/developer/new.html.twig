{% extends 'base.html.twig' %}

{% block body %}
    <h1>Developer creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
    {{ form_end(form) }}

    <a href="{{ path('developer_index') }}" class="btn btn-warning float-left" role="button">Back to the list</a>
{% endblock %}
